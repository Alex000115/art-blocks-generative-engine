# art-blocks-generative-engine
An expert-level, On-chain Generative Art engine. Features a "Script-Storage" model where the artist's p5.js or Three.js code is stored directly in the contract metadata, and the unique NFT output is deterministically generated at the moment of minting using the transaction hash as a seed.
